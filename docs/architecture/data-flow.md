# Data Flow Architecture

Detailed data flow diagrams showing how requests and responses move through the MCP-CLI system in different operational modes.

---

## Table of Contents

- [Chat Mode Flow](#chat-mode-flow)
- [Query Mode Flow](#query-mode-flow)
- [Interactive Mode Flow](#interactive-mode-flow)
- [Template Execution Flow](#template-execution-flow)
- [MCP Server Protocol Flow](#mcp-server-protocol-flow)
- [Tool Execution Flow](#tool-execution-flow)
- [Error Handling Flow](#error-handling-flow)

---

## Chat Mode Flow

### Complete Request-Response Cycle

```
User Types Message
    │
    ▼
┌─────────────────────────────────────────────────────┐
│ 1. Input Handling (core/chat/ui.go)                 │
│    • Read user input                                 │
│    • Detect commands (/help, /clear, etc.)          │
│    • Validate input                                  │
└─────────────────────┬───────────────────────────────┘
                      │
                      ├──► Is Command? ──┐
                      │                   │
                      NO                  YES
                      │                   │
                      ▼                   ▼
        ┌─────────────────────┐  ┌───────────────────┐
        │ 2. Message          │  │ Execute Command   │
        │    Processing       │  │ (/help, /clear,   │
        │                     │  │  /history, etc.)  │
        └──────────┬──────────┘  └───────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────┐
│ 3. Context Management (core/chat/context.go)        │
│    • Add user message to conversation history       │
│    • Check token budget                             │
│    • Trim old messages if needed                    │
│    • Preserve system prompt                         │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 4. Request Building (core/chat/manager.go)          │
│    • Build CompletionRequest                        │
│    • Include conversation history                   │
│    • Add available tools (from MCP servers)         │
│    • Set parameters (temp, max_tokens)              │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 5. Provider Selection (providers/ai/factory.go)     │
│    • Determine provider (CLI flag or config)        │
│    • Create provider instance                       │
│    • Validate configuration                         │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 6. AI API Call (providers/ai/clients/)              │
│    • Stream: StreamCompletion()                     │
│    • Rate limiting                                  │
│    • HTTP request to AI provider                    │
│    • Authenticate with API key                      │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 7. Response Streaming (providers/ai/streaming/)     │
│    • Receive SSE chunks                             │
│    • Parse chunk data                               │
│    • Write to output buffer                         │
│    • Display incrementally                          │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
              Has Tool Calls?
                      │
              ┌───────┴───────┐
              NO             YES
              │               │
              ▼               ▼
    ┌─────────────┐  ┌────────────────────────────┐
    │ 8. Display  │  │ 9. Tool Execution          │
    │    Response │  │    (core/chat/tools.go)    │
    │             │  │    • Parse tool calls      │
    └──────┬──────┘  │    • Execute in parallel   │
           │         │    • Collect results       │
           │         └────────────┬───────────────┘
           │                      │
           │                      ▼
           │         ┌────────────────────────────┐
           │         │ 10. Continue with Results  │
           │         │     • Add tool results     │
           │         │     • Request continuation │
           │         │     • Get final response   │
           │         └────────────┬───────────────┘
           │                      │
           └──────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 11. Context Update (core/chat/context.go)           │
│     • Add assistant response to history             │
│     • Update token count                            │
│     • Save tool use history                         │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
                 Ready for Next Message
```

### Streaming Detail

```
AI Provider Response Stream
    │
    ▼
┌──────────────────────────────────┐
│ SSE Data Chunks                   │
│ data: {"delta":{"content":"H"}}  │
│ data: {"delta":{"content":"i"}}  │
│ data: {"delta":{"content":" "}}  │
│ data: [DONE]                     │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ Stream Processor                  │
│ (providers/ai/streaming/)         │
│                                   │
│ chunks := make(chan string)       │
│ for chunk := range stream {       │
│     chunks <- chunk.Content       │
│ }                                 │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ Output Writer                     │
│ (presentation/streaming/)         │
│                                   │
│ for chunk := range chunks {       │
│     fmt.Print(chunk)              │
│     fullContent += chunk          │
│ }                                 │
└───────────────────────────────────┘
```

---

## Query Mode Flow

### Single-Shot Request-Response

```
User Command: mcp-cli query "question"
    │
    ▼
┌─────────────────────────────────────────────────────┐
│ 1. Command Parsing (cmd/query.go)                   │
│    • Parse flags (--provider, --model, --json)      │
│    • Get query text (args, stdin, or file)          │
│    • Validate input                                 │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 2. Service Creation (services/query/service.go)     │
│    • Load configuration                             │
│    • Initialize provider factory                    │
│    • Connect MCP servers (if specified)             │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 3. Request Building (core/query/handler.go)         │
│    • Create CompletionRequest                       │
│    • Single message (no history)                    │
│    • Add tools if --server specified                │
│    • Set parameters                                 │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 4. Provider Execution                               │
│    • Select provider                                │
│    • Execute with retry logic                       │
│    • Handle rate limiting                           │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 5. AI API Call                                      │
│    • POST /chat/completions or equivalent           │
│    • Wait for complete response (no streaming)      │
│    • Parse response                                 │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
              Has Tool Calls?
                      │
              ┌───────┴───────┐
              NO             YES
              │               │
              ▼               ▼
    ┌─────────────┐  ┌────────────────────────────┐
    │ 6a. Format  │  │ 6b. Execute Tools          │
    │     Output  │  │     • Call MCP servers     │
    │             │  │     • Collect results      │
    └──────┬──────┘  │     • Make follow-up call  │
           │         └────────────┬───────────────┘
           │                      │
           └──────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 7. Output Formatting (presentation/*)                │
│    • Text: Print response content                   │
│    • JSON: Convert to JSON structure                │
│    • File: Write to specified file                  │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 8. Exit                                             │
│    • Return exit code                               │
│    • 0 = success                                    │
│    • 1 = error                                      │
└─────────────────────────────────────────────────────┘
```

### Piped Input Handling

```
Command: cat file.txt | mcp-cli query

stdin (file.txt content)
    │
    ▼
┌──────────────────────────────────┐
│ Input Reader                      │
│ (cmd/query.go)                   │
│                                   │
│ if isStdin() {                    │
│     content = readStdin()         │
│ } else {                          │
│     content = args[0]             │
│ }                                 │
└────────────┬─────────────────────┘
             │
             ▼
┌──────────────────────────────────┐
│ Query Processor                   │
│                                   │
│ query = buildQuery(content)       │
│ result = execute(query)           │
└────────────┬─────────────────────┘
             │
             ▼
        stdout (result)
```

---

## Interactive Mode Flow

### REPL (Read-Eval-Print Loop)

```
Start Interactive Mode
    │
    ▼
┌─────────────────────────────────────────────────────┐
│ 1. Initialization (cmd/interactive.go)              │
│    • Load configuration                             │
│    • Connect to MCP servers                         │
│    • List available tools                           │
│    • Display welcome message                        │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
        ┌─────────────────────────┐
        │ REPL Loop               │
        │                         │
        │ while true {            │
        │   input = readLine()    │
        │   process(input)        │
        │   display(result)       │
        │ }                       │
        └──────────┬──────────────┘
                   │
    ┌──────────────┴──────────────┐
    │                             │
    ▼                             ▼
┌─────────┐              ┌─────────────────┐
│ Command │              │ Unknown Input   │
│         │              │ (show help)     │
└────┬────┘              └─────────────────┘
     │
     ├──► /help      ──► Display commands
     ├──► /tools     ──► List available tools
     ├──► /tools-all ──► Show tool details
     ├──► /ping      ──► Test server connection
     ├──► /call      ──► Execute tool (see below)
     ├──► /clear     ──► Clear screen
     └──► /exit      ──► Exit program
```

### Tool Call Flow

```
User: /call filesystem read_file {"path": "test.txt"}
    │
    ▼
┌─────────────────────────────────────────────────────┐
│ 1. Parse Command (core/interactive/commands.go)     │
│    • Parse: server=filesystem                       │
│    • Parse: tool=read_file                          │
│    • Parse: args={"path": "test.txt"}               │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 2. Validate (core/interactive/service.go)           │
│    • Server exists?                                 │
│    • Tool available on server?                      │
│    • Arguments match schema?                        │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 3. Build JSON-RPC Request (providers/mcp/messages/) │
│    {                                                │
│      "jsonrpc": "2.0",                              │
│      "id": 1,                                       │
│      "method": "tools/call",                        │
│      "params": {                                    │
│        "name": "read_file",                         │
│        "arguments": {"path": "test.txt"}            │
│      }                                              │
│    }                                                │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 4. Send to MCP Server (providers/mcp/transport/)    │
│    • Write JSON to server stdin                     │
│    • Read JSON-RPC response from stdout             │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 5. Parse Response                                   │
│    {                                                │
│      "jsonrpc": "2.0",                              │
│      "id": 1,                                       │
│      "result": {                                    │
│        "content": "File contents here..."           │
│      }                                              │
│    }                                                │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 6. Display Result (presentation/console/)           │
│    • Format as colored JSON                         │
│    • Syntax highlighting                            │
│    • Pretty printing                                │
└─────────────────────────────────────────────────────┘
```

---

## Workflow Execution Flow

### Multi-Step Workflow

```
Command: mcp-cli --template code_review
    │
    ▼
┌─────────────────────────────────────────────────────┐
│ 1. Template Loading (services/template/parser.go)   │
│    • Find template file                             │
│    • Parse YAML                                     │
│    • Validate structure                             │
│    • Load dependencies                              │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 2. Variable Initialization                          │
│    • Load input data (--input-data or stdin)        │
│    • Initialize variable context                    │
│    • Set up output variables                        │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
         ┌────────────────────────┐
         │ For Each Step          │
         └────────┬───────────────┘
                  │
    ┌─────────────┼─────────────┐
    │             │             │
    ▼             ▼             ▼
┌────────┐  ┌──────────┐  ┌──────────┐
│Evaluate│  │Substitute│  │  Select  │
│Condition│  │Variables │  │ Provider │
└───┬────┘  └────┬─────┘  └────┬─────┘
    │            │             │
    └────────────┴─────────────┘
                  │
         Skip if condition false
                  │
                  ▼
┌─────────────────────────────────────────────────────┐
│ 3. Step Execution (services/template/executor.go)   │
│                                                     │
│ Step Types:                                         │
│ • prompt: AI completion                             │
│ • template: Call sub-template                       │
│ • tool: Direct tool call                            │
│ • for_each: Loop over array                         │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
          Step Type = prompt
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 4. Provider Execution                               │
│    • Build completion request                       │
│    • Include tools if specified                     │
│    • Call AI provider                               │
│    • Handle tool calls recursively                  │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 5. Store Output                                     │
│    • Save response to output variable               │
│    • Add to variable context                        │
│    • Available for next steps                       │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
              More Steps?
                      │
              ┌───────┴───────┐
              YES            NO
              │               │
              │               ▼
              └────────►┌──────────────────┐
                        │ 6. Final Output  │
                        │    Return results│
                        └──────────────────┘
```

### Variable Substitution Example

```yaml
# Workflow step
steps:
  - name: analyze
    prompt: "Analyze this {{input_data.language}} code: {{input_data.code}}"
    output: analysis
  
  - name: suggest
    prompt: "Based on this analysis: {{analysis}}, suggest improvements"
```

```
Execution:

Input Variables:
  input_data.language = "python"
  input_data.code = "def foo():\n  pass"

Step 1 Substitution:
  "Analyze this python code: def foo():\n  pass"
  
Step 1 Execution → Response:
  "This function is empty and should be implemented..."

Variable Context After Step 1:
  analysis = "This function is empty..."

Step 2 Substitution:
  "Based on this analysis: This function is empty..., suggest improvements"
  
Step 2 Execution → Final Response
```

---

## MCP Server Protocol Flow

### Server Startup and Initialization

```
mcp-cli serve config/runas/server.yaml
    │
    ▼
┌─────────────────────────────────────────────────────┐
│ 1. Parse Server Config                              │
│    • Load runas YAML                                │
│    • Extract tool definitions                       │
│    • Load referenced templates                      │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 2. Start JSON-RPC Server                            │
│    • Listen on stdio                                │
│    • Set up message handlers                        │
│    • Register protocol methods                      │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 3. Wait for Initialize                              │
│    Client → Server:                                 │
│    {                                                │
│      "method": "initialize",                        │
│      "params": {                                    │
│        "protocolVersion": "2024-11-05",             │
│        "clientInfo": {...}                          │
│      }                                              │
│    }                                                │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 4. Send Initialize Response                         │
│    Server → Client:                                 │
│    {                                                │
│      "result": {                                    │
│        "protocolVersion": "2024-11-05",             │
│        "serverInfo": {                              │
│          "name": "code-tools",                      │
│          "version": "1.0.0"                         │
│        },                                           │
│        "capabilities": {                            │
│          "tools": {}                                │
│        }                                            │
│      }                                              │
│    }                                                │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
                 Server Ready
```

### Tool Discovery

```
Client Request: List Tools
    │
    ▼
┌─────────────────────────────────────────────────────┐
│ Client → Server:                                    │
│ {                                                   │
│   "method": "tools/list",                           │
│   "id": 2                                           │
│ }                                                   │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ Server Processes Request                            │
│    • Load tool definitions from config              │
│    • Map to MCP tool schema                         │
│    • Include parameter schemas                      │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ Server → Client:                                    │
│ {                                                   │
│   "result": {                                       │
│     "tools": [                                      │
│       {                                             │
│         "name": "analyze_code",                     │
│         "description": "Analyze code for bugs",     │
│         "inputSchema": {                            │
│           "type": "object",                         │
│           "properties": {                           │
│             "code": {"type": "string"},             │
│             "language": {"type": "string"}          │
│           },                                        │
│           "required": ["code"]                      │
│         }                                           │
│       }                                             │
│     ]                                               │
│   }                                                 │
│ }                                                   │
└─────────────────────────────────────────────────────┘
```

### Tool Execution

```
Client Calls Tool
    │
    ▼
┌─────────────────────────────────────────────────────┐
│ Client → Server:                                    │
│ {                                                   │
│   "method": "tools/call",                           │
│   "params": {                                       │
│     "name": "analyze_code",                         │
│     "arguments": {                                  │
│       "code": "def foo(): pass",                    │
│       "language": "python"                          │
│     }                                               │
│   },                                                │
│   "id": 3                                           │
│ }                                                   │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 1. Validate Arguments                               │
│    • Check against tool schema                      │
│    • Verify required parameters                     │
│    • Type checking                                  │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 2. Map to Template                                  │
│    • Tool "analyze_code" → template "code_analyzer" │
│    • Parameters → input_data variables              │
│    • {"code": "...", "language": "..."} →           │
│      {{input_data.code}}, {{input_data.language}}   │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 3. Execute Template                                 │
│    • Load template                                  │
│    • Execute steps with AI provider                 │
│    • Collect results                                │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 4. Format Response                                  │
│    • Convert template output to MCP format          │
│    • Include any errors                             │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ Server → Client:                                    │
│ {                                                   │
│   "result": {                                       │
│     "content": [                                    │
│       {                                             │
│         "type": "text",                             │
│         "text": "Analysis: This function..."        │
│       }                                             │
│     ]                                               │
│   },                                                │
│   "id": 3                                           │
│ }                                                   │
└─────────────────────────────────────────────────────┘
```

---

## Tool Execution Flow

### AI-Initiated Tool Call

```
AI Response Contains Tool Call
    │
    ▼
┌─────────────────────────────────────────────────────┐
│ 1. Parse Tool Call                                  │
│    {                                                │
│      "tool_calls": [                                │
│        {                                            │
│          "id": "call_abc123",                       │
│          "function": {                              │
│            "name": "read_file",                     │
│            "arguments": "{\"path\":\"test.txt\"}"   │
│          }                                          │
│        }                                            │
│      ]                                              │
│    }                                                │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 2. Identify Server                                  │
│    • Match tool name to MCP server                  │
│    • "read_file" → filesystem server                │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 3. Execute on MCP Server                            │
│    • Build JSON-RPC request                         │
│    • Send to server                                 │
│    • Wait for response                              │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 4. Format Tool Result                               │
│    {                                                │
│      "role": "tool",                                │
│      "tool_call_id": "call_abc123",                 │
│      "content": "File contents: ..."                │
│    }                                                │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│ 5. Continue AI Conversation                         │
│    • Add tool result to messages                    │
│    • Request continuation from AI                   │
│    • AI processes tool result                       │
│    • Returns final response to user                 │
└─────────────────────────────────────────────────────┘
```

---

## Error Handling Flow

### Error Classification and Handling

```
Error Occurs
    │
    ▼
┌─────────────────────────────────────────────────────┐
│ Error Classification                                │
└─────────────────────┬───────────────────────────────┘
                      │
    ┌─────────────────┼─────────────────┬────────────┐
    │                 │                 │            │
    ▼                 ▼                 ▼            ▼
┌──────────┐    ┌──────────┐    ┌──────────┐  ┌──────────┐
│  User    │    │ Provider │    │ Network  │  │ System   │
│  Error   │    │  Error   │    │  Error   │  │  Error   │
└────┬─────┘    └────┬─────┘    └────┬─────┘  └────┬─────┘
     │               │               │             │
     │               │               │             │
     ▼               ▼               ▼             ▼
┌──────────┐    ┌──────────┐    ┌──────────┐  ┌──────────┐
│ Return   │    │ Retry    │    │ Retry    │  │ Log &    │
│ Helpful  │    │ with     │    │ with     │  │ Exit     │
│ Message  │    │ Backoff  │    │ Backoff  │  │          │
└──────────┘    └──────────┘    └──────────┘  └──────────┘
```

### Retry Flow with Exponential Backoff

```
Operation Fails
    │
    ▼
┌─────────────────────────────────────────────────────┐
│ Check if Retryable                                  │
│ • Network timeout?           → Yes                  │
│ • Rate limit (429)?          → Yes                  │
│ • Server error (500)?        → Yes                  │
│ • Auth error (401)?          → No                   │
│ • Invalid request (400)?     → No                   │
└─────────────────────┬───────────────────────────────┘
                      │
              ┌───────┴───────┐
              NO             YES
              │               │
              ▼               ▼
        ┌──────────┐    ┌──────────────────┐
        │ Return   │    │ Retry Counter    │
        │ Error    │    │ attempt++        │
        └──────────┘    └────────┬─────────┘
                                 │
                        ┌────────┴────────┐
                        │                 │
                   attempt > max?        NO
                        │                 │
                       YES                ▼
                        │         ┌───────────────┐
                        │         │ Calculate     │
                        │         │ Backoff       │
                        │         │ delay = base  │
                        │         │   * 2^attempt │
                        │         └───────┬───────┘
                        │                 │
                        │                 ▼
                        │         ┌───────────────┐
                        │         │ Sleep(delay)  │
                        │         └───────┬───────┘
                        │                 │
                        │                 ▼
                        │         ┌───────────────┐
                        │         │ Retry         │
                        │         │ Operation     │
                        │         └───────┬───────┘
                        │                 │
                        │                 │
                        └─────────────────┘
                                 │
                                 ▼
                          Success or
                          Permanent Failure
```

---

**Data flows documented!** Continue to [API & Domain](api-domain.md) for interface specifications. 📊
